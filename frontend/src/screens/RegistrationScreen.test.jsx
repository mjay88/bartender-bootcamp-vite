import { it, describe, expect } from "vitest";
import { render, screen } from "@testing-library/react";

describe("Form Screen", () => {
	it("should call onsubmit when the form is valid with the correct data");
});
